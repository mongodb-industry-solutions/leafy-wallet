import { MobileShell } from '@/components/stage/MobileShell'

/** Phone-only route: the wallet app full screen, for opening the demo on a real handset. */
export default function Mobile() {
  return <MobileShell />
}
