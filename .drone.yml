---
kind: pipeline
type: kubernetes
name: staging-combined

trigger:
  branch:
    - staging
  event:
    - push

steps:
  - name: publish-backend
    image: plugins/kaniko-ecr
    resources:
      requests:
        cpu: 1000
        memory: 2048Mi
      limits:
        cpu: 2000
        memory: 4096Mi
    settings:
      compressed_caching: false
      snapshot_mode: redo
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/leafy-wallet-backend
      dockerfile: Dockerfile.backend
      context: .
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}
        - latest
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key

  - name: publish-frontend
    image: plugins/kaniko-ecr
    resources:
      requests:
        cpu: 1000
        memory: 2048Mi
      limits:
        cpu: 2000
        memory: 4096Mi
    settings:
      compressed_caching: false
      snapshot_mode: redo
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/leafy-wallet-frontend
      dockerfile: Dockerfile.frontend
      context: .
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}
        - latest
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key

  - name: publish-local-store
    image: plugins/kaniko-ecr
    resources:
      requests:
        cpu: 1000
        memory: 2048Mi
      limits:
        cpu: 2000
        memory: 4096Mi
    settings:
      compressed_caching: false
      snapshot_mode: redo
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/leafy-wallet-local-store
      dockerfile: leafy-local-store/Dockerfile
      context: leafy-local-store
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}
        - latest
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key

  - name: deploy-combined-staging
    image: public.ecr.aws/kanopy/drone-helm:v3
    settings:
      chart: mongodb/web-app
      chart_version: 4.30.0
      add_repos: [mongodb=https://10gen.github.io/helm-charts]
      namespace: industrysolutions
      release: leafy-wallet-frontend
      values_files: ["environment/staging.yaml"]
      # sidecarContainers indices follow environment/staging.yaml: 0 backend,
      # 1 leafy-local-store, 2 objectbox-sync-server.
      values:
        - "image.tag=git-${DRONE_COMMIT_SHA:0:7}"
        - "image.repository=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/leafy-wallet-frontend"
        - "sidecarContainers[0].image=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/leafy-wallet-backend:git-${DRONE_COMMIT_SHA:0:7}"
        - "sidecarContainers[1].image=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/leafy-wallet-local-store:git-${DRONE_COMMIT_SHA:0:7}"
        - "ingress.enabled=true"
        - "ingress.hosts[0]=leafy-wallet-frontend.industrysolutions.staging.corp.mongodb.com"
        - "mesh.enabled=true"
        - "resources.limits.cpu=500m"
        - "resources.limits.memory=1024Mi"
        - "resources.requests.cpu=100m"
        - "resources.requests.memory=256Mi"
        - "sidecarContainers[0].resources.limits.cpu=500m"
        - "sidecarContainers[0].resources.limits.memory=1024Mi"
        - "sidecarContainers[0].resources.requests.cpu=100m"
        - "sidecarContainers[0].resources.requests.memory=256Mi"
        - "sidecarContainers[1].resources.limits.cpu=500m"
        - "sidecarContainers[1].resources.limits.memory=512Mi"
        - "sidecarContainers[1].resources.requests.cpu=100m"
        - "sidecarContainers[1].resources.requests.memory=256Mi"
        - "sidecarContainers[2].resources.limits.cpu=500m"
        - "sidecarContainers[2].resources.limits.memory=1024Mi"
        - "sidecarContainers[2].resources.requests.cpu=100m"
        - "sidecarContainers[2].resources.requests.memory=256Mi"
      api_server: https://api.staging.corp.mongodb.com
      kubernetes_token:
        from_secret: staging_kubernetes_token

---
kind: pipeline
type: kubernetes
name: production-combined

trigger:
  branch:
    - main
  event:
    - push

steps:
  - name: publish-backend-prod
    image: plugins/kaniko-ecr
    resources:
      requests:
        cpu: 1000
        memory: 2048Mi
      limits:
        cpu: 2000
        memory: 4096Mi
    settings:
      compressed_caching: false
      snapshot_mode: redo
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/leafy-wallet-backend
      dockerfile: Dockerfile.backend
      context: .
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}
        - latest
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key

  - name: publish-frontend-prod
    image: plugins/kaniko-ecr
    resources:
      requests:
        cpu: 1000
        memory: 2048Mi
      limits:
        cpu: 2000
        memory: 4096Mi
    settings:
      compressed_caching: false
      snapshot_mode: redo
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/leafy-wallet-frontend
      dockerfile: Dockerfile.frontend
      context: .
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}
        - latest
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key

  - name: publish-local-store-prod
    image: plugins/kaniko-ecr
    resources:
      requests:
        cpu: 1000
        memory: 2048Mi
      limits:
        cpu: 2000
        memory: 4096Mi
    settings:
      compressed_caching: false
      snapshot_mode: redo
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/leafy-wallet-local-store
      dockerfile: leafy-local-store/Dockerfile
      context: leafy-local-store
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}
        - latest
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key

  - name: deploy-combined-prod
    image: public.ecr.aws/kanopy/drone-helm:v3
    settings:
      chart: mongodb/web-app
      chart_version: 4.30.0
      add_repos: [mongodb=https://10gen.github.io/helm-charts]
      namespace: industrysolutions
      release: leafy-wallet-frontend
      values_files: ["environment/production.yaml"]
      # sidecarContainers indices follow environment/production.yaml: 0 backend,
      # 1 leafy-local-store, 2 objectbox-sync-server.
      values:
        - "image.tag=git-${DRONE_COMMIT_SHA:0:7}"
        - "image.repository=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/leafy-wallet-frontend"
        - "sidecarContainers[0].image=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/leafy-wallet-backend:git-${DRONE_COMMIT_SHA:0:7}"
        - "sidecarContainers[1].image=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/leafy-wallet-local-store:git-${DRONE_COMMIT_SHA:0:7}"
        - "ingress.enabled=true"
        - "ingress.hosts[0]=leafy-wallet-frontend.industrysolutions.prod.corp.mongodb.com"
        - "mesh.enabled=true"
        - "resources.limits.cpu=500m"
        - "resources.limits.memory=1024Mi"
        - "resources.requests.cpu=150m"
        - "resources.requests.memory=256Mi"
        - "sidecarContainers[0].resources.limits.cpu=500m"
        - "sidecarContainers[0].resources.limits.memory=1024Mi"
        - "sidecarContainers[0].resources.requests.cpu=150m"
        - "sidecarContainers[0].resources.requests.memory=256Mi"
        - "sidecarContainers[1].resources.limits.cpu=500m"
        - "sidecarContainers[1].resources.limits.memory=512Mi"
        - "sidecarContainers[1].resources.requests.cpu=150m"
        - "sidecarContainers[1].resources.requests.memory=256Mi"
        - "sidecarContainers[2].resources.limits.cpu=500m"
        - "sidecarContainers[2].resources.limits.memory=1024Mi"
        - "sidecarContainers[2].resources.requests.cpu=150m"
        - "sidecarContainers[2].resources.requests.memory=256Mi"
      api_server: https://api.prod.corp.mongodb.com
      kubernetes_token:
        from_secret: prod_kubernetes_token
